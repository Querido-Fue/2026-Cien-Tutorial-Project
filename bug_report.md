# 게임 전체 기능 검증 보고서

검증일: 2026-08-30 KST  
검증 기준: `main` / `bf09787`  
검증 방식: Codex 인앱 Chromium 브라우저 제어, Node 회귀 테스트, 정적 자산·구조 검사,
결정론적 전투 시뮬레이션

## 1. 결론

- 검증 중 사용자 입력을 막는 런타임 결함 1건과 반응형 텍스트 결함 1건을 발견해 수정했다.
- 수정 뒤 확인한 범위에서 재현되는 미해결 런타임 결함은 0건이다.
- Node 테스트는 232/232 통과했고 브라우저 콘솔 오류는 전체 수동 시나리오에서 0건이었다.
- 전투 시뮬레이션 8개는 구조 실패 0건, preview/실행 불일치 0건으로 끝났다.
- 공개 배포는 코드 결함과 별개인 권리 증빙 5건 때문에 계속 차단되어야 한다.
- `계속하기`는 현재 항상 비활성인 의도 미확정 기능이며, 네이티브 Pointer Lock과 OS
  `Alt+F4` 경로는 자동화 제약 때문에 실제 장치 수동 확인이 한 번 더 필요하다.

## 2. 실행 환경과 범위

| 항목 | 값 |
| --- | --- |
| 브라우저 | Codex 인앱 Chromium |
| 기본 캡처 | 1280×720 |
| 반응형 캡처 | 1024×768, 1600×720 |
| 로컬 서버 | `http://127.0.0.1:49329/` |
| 테스트 런타임 | Node.js 22.19.0 |
| 에셋 | PNG 84개, MP3 26개 |

브라우저 자동화의 Pointer Lock 구현은 잠금된 `document.documentElement` 대신 별도 sentinel을
반환하므로 정상 잠금 상태를 끝까지 재현할 수 없었다. 전투 수동 검증에는 생성물에만 임시로
Pointer Lock 미지원 폴백을 주입했다. 이 파일은 Git 추적 대상이 아니며 최종 `build:web`으로
제거한다. 제품 소스의 미지원 폴백과 실제 UI/게임 명령 경로는 그대로 사용했다.

## 3. 수동 브라우저 검증

| 영역 | 확인 내용 | 결과 |
| --- | --- | --- |
| 첫 실행 | 로고, `화면을 클릭해주세요`, KST 버전 표기 | 통과 |
| 타이틀 | 새 게임, 갤러리, 체인지로그 진입과 복귀 | 통과 |
| 스타터 | 활·화살/인형탈 두 카드, 같은 씬 전환, 인벤토리 모핑 | 통과 |
| 컷씬 | 오프닝 9장 다음/완료, 스킵 | 통과 |
| 전투 안내 | 한 번에 한 메시지, 6단계 화면 클릭 전환, Escape 건너뛰기 | 수정 후 통과 |
| 전투 이동 | 키보드 경로 선택, 3칸 확정, 카메라 추적 | 통과 |
| 행동 메뉴 | 이동 확정 뒤 공격·회복·대기 전환, 공격 대상 선택 | 통과 |
| 턴/HP | 로라 공격, 플레이어·로라 HP 수치와 바 변화 | 통과 |
| Pause | Escape 진입, 계속하기로 전투 상태 복귀 | 통과 |
| 인벤토리 | 인형탈 호버 배너, 별도 엔진 tooltip 미표시 | 통과 |
| 맵 기록 | 일기 페이지 획득, 장면 위 기록책 팝업, 본문 해금, 닫기 | 통과 |
| 갤러리 | 업적·로라 일기·개발자 기록·엔딩·컷씬 책갈피 | 통과 |
| 체인지로그 | 버전 `8/30 HH:mm` 형식, 페이지 이동 영역, 1024×768 줄간격 | 수정 후 통과 |
| 반응형 | 1024×768과 1600×720에서 책·버튼 화면 내 유지 | 통과 |
| WebGL 표현 | 촛불, 먼지, 후처리, UI 선명도, 검정 바깥 배경 | 통과 |
| 오류 관찰 | 위 흐름 전체의 브라우저 console | 오류 0건 |

로컬 에셋 로딩이 매우 빨라 로딩 바의 중간 퍼센트는 수동 캡처로 고정하지 못했다. 대신
`tutorial_loading.test.mjs`가 75% 문구와 실제 픽셀 바, 에셋·메타 동시 완료 조건을 검사한다.

## 4. 발견하고 수정한 결함

### QA-001 — `top` 레이어 UI가 마우스 입력을 받지 못함

- 심각도: 높음
- 증상: 전투 안내에서 화면 클릭으로 다음 단계가 넘어가지 않았다. 같은 원인으로 기록 팝업 등
  `top` 캔버스에 배치된 버튼도 기본 UI 포커스에서 클릭되지 않을 수 있었다.
- 원인: `ButtonElement`가 렌더 레이어 이름과 입력 포커스 이름이 정확히 같을 때만 입력을
  허용했지만, `top`은 시각 캔버스이고 입력 의미상 `ui`의 서브레이어다.
- 수정: `top → ui` 입력 도메인 매핑을 `_ui_input_focus_policy.js`로 분리했다. 엔진 overlay가
  포커스를 독점할 때에는 이전처럼 차단된다.
- 회귀: `ui_input_focus_policy.test.mjs` 3개와 실제 6단계 화면 클릭, 기록 팝업 닫기 검증 통과.
- 반영 커밋: `1073f93 fix: 최상단 UI 버튼 입력 포커스 복구`

### QA-002 — 체인지로그 두 줄 본문이 겹침

- 심각도: 보통
- 증상: 특히 1024×768에서 각 변경 항목의 두 줄 요약이 서로 겹쳐 읽기 어려웠다.
- 원인: 줄간격을 글꼴 크기와 무관한 화면 높이 2.2%로만 계산했다.
- 수정: 기존 비율값과 실제 픽셀 글꼴 높이의 1.3배 중 큰 값을 사용한다.
- 회귀: 18px 폰트의 최소 1.3배 간격 단위 테스트와 1024×768 브라우저 재캡처 통과.
- 반영 커밋: `bf09787 fix: 체인지로그 본문 줄간격 보정`

## 5. 자동 검증 결과

### 전체 회귀와 구조 검사

- `npm test`: 232/232 통과
- `check:assets`: PNG 84, MP3 26, 코드 폴백 0, 오디오 폴백 1, 경고 0
- `check:repo`: 오류 0, 경고 0
- `check:responsibilities`: 소스 284, 500줄 초과 25, 기존 부채 11, 정책 위반 0
- `check:release`: JS 253, import 620, 에셋 257, 구조 검사 성공
- `npm run build:web`: 성공

테스트는 전투 모델과 두 층, 포탈·벽·타일 이벤트, 아이템, 기록, 업적, 엔딩, 저장 schema,
오디오 버스, 스프라이트/그림자, 카메라와 연속 휠 줌, 포커스/Pointer Lock, 타이틀 전환,
WebGL FBO·Bloom·폴백, 웹 최신 버전 재접속을 포함한다.

### 결정론적 밸런스 시뮬레이션

`npm run simulate:balance -- --json --no-write` 결과:

- 스타터 2종 × agent 4종 = 8개 시나리오
- 명령 223개, preview 검사 171개
- 구조 실패 0건, 불변식 위반 0건, preview 불일치 0건
- 완료 2, 패배 4, 12턴 제한 2
- 엔딩: 실패 6, 일반 1, 진엔딩 1

활 스타터의 네 휴리스틱은 모두 패배했다. 모델 오류는 아니지만 목표 난이도와 맞는지는 별도
밸런스 판단이 필요하다.

## 6. 미확정 기능과 잔여 위험

### 기능/UX 확인 필요

1. `계속하기`는 메뉴에 보이지만 `TutorialNonbattleViewModelFactory`가 `canContinue: false`를
   고정하고 전투 세션 영속 저장 경로가 없다. 의도한 비활성 자리표시자인지, 실제 중단 저장을
   구현해야 하는지 기획 확인이 필요하다.
2. `build:web`과 일부 직접 Node 실행은 `project/package.json`에 module type이 없다는
   `MODULE_TYPELESS_PACKAGE_JSON` 경고를 출력한다. 빌드와 런타임은 성공하지만 Node 도구 성능
   경고가 남는다. NW.js 패키지 의미가 바뀔 수 있어 단순히 `type: module`을 추가하지 않았다.

### 자동화 한계

1. 네이티브 Pointer Lock의 실제 잠금/해제, 화면 가장자리 ESC 안내의 물리 커서 동작,
   포커스 아웃 상태의 OS `Alt+F4` 종료 팝업 입력 우선순위는 실제 Chromium/NW.js 수동 확인이
   필요하다. 관련 상태머신·입력 소비 자동 테스트는 모두 통과했다.
2. BGM·호흡·효과음의 재생 정책과 파일 무결성은 검증했지만 음량의 주관적 청감은 헤드폰
   수동 확인 범위다.
3. 일반/진/실패 엔딩은 모델 시뮬레이션과 뷰 테스트로 검증했고, 각 엔딩까지의 실시간 전 구간
   수동 플레이는 이번 브라우저 세션에서 반복하지 않았다.

## 7. 공개 배포 차단 사항

아래는 런타임 버그가 아니라 소유자 확인이 필요한 권리 증빙이다. 해결 전 공개 배포하지 않는다.

1. 저장소 코드 라이선스와 배포 권한
2. 제공 아트 원본의 저작자·라이선스·양도/사용 허가
3. 제공 오디오 원본의 제작자·배포 허가
4. 런타임 에셋 복사본이 상속하는 원본 권리
5. 외부 작품을 참조한 아이템 아이콘의 사용 허가 또는 비침해 대체 아트 검토

## 8. 다음 수동 릴리스 스모크 권장 순서

실제 배포 후보 NW.js/Chromium에서 다음 네 항목만 추가로 확인하면 이번 자동화 공백을 닫을 수
있다.

1. 첫 클릭 Pointer Lock → 가장자리 1초 유지 → ESC 해제 → 재클릭 잠금 복구
2. 포커스 아웃 블러 상태에서 `Alt+F4` → 종료 팝업 버튼이 첫 클릭을 우선 소비
3. BGM·1페이즈 무호흡·2페이즈 50% 호흡·효과음 청감
4. 일반 엔딩과 진엔딩을 각각 한 번 실제 조작으로 완료한 뒤 갤러리 해금 확인
